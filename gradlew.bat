@echo off
REM Minimal gradlew.bat stub. If running locally you should generate the gradle wrapper or use Android Studio to build.
echo This is a stub gradlew.bat. Generate the gradle wrapper locally if you need to run this.