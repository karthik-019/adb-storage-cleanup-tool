#!/usr/bin/env sh
# Minimal gradlew stub - generate gradle wrapper locally if missing
echo "This is a gradlew stub. If this file is executed on CI, ensure gradle-wrapper.jar exists in gradle/wrapper/."